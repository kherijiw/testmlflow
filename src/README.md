
# install environement sous unix 
```bash
pip install pipenv
pipenv install 
pipenv shell
chmod u+x pipeline.sh
```

# install environement sous wind

```powershell
pip install pipenv
pipenv install 
pipenv shell
./pipeline.bat
```
